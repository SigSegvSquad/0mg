class OrderDetails{
  String orderId;
  var userDetails;
  var orderContent;
  String orderStatus;
  int amount;
  String date;

  OrderDetails(this.orderId,this.userDetails, this.orderContent, this.orderStatus, this.amount,
      this.date);
}